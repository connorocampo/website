import React from "react"

export default ({ children }) => (
  <div
    style={{ height: '100%', margin: '0 auto', marginTop: '1rem', maxWidth: '1100px', padding: '2rem 2rem 0 2rem' }}>
    {children}
  </div>
)